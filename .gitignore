node_modules/
.env
.env.local
*.log
.DS_Store
Thumbs.db
dist/
build/

# Deploy-Scripts (enthalten lokale Pfade und Credentials)
push.bat
push.sh

# Backup-Dateien
*.bak
*.tawk_bak

# Temporäre/unbekannte Dateien ohne Endung im Root
download

# Firebase Service Account Credentials (NIEMALS committen!)
serviceAccount*.json
*firebase-adminsdk*.json
firestore-backup.json

# Vercel-Config (wir nutzen Firebase Hosting)
# vercel.json bleibt im Repo für den Fall eines Plattform-Wechsels,
# aber wird beim Firebase-Deploy ignoriert (siehe firebase.json → ignore)
